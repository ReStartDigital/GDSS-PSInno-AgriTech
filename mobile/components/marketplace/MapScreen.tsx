import { View, Text } from "react-native";

export function MapScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-gray-100">
      <Text className="text-gray-600">Map view — requires native build</Text>
    </View>
  );
}
