FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
# Source mounted as volume — ts-node-dev picks up changes live
COPY . .
EXPOSE 3000
# ts-node-dev: restarts server on file change, skips full compile
CMD ["npm", "run", "dev"]